# .dd 파일 포맷 명세 (rule)

ASML ID dump(`.tdf`) 안에 들어있는 `.dd` 파일의 구조 규칙 정리.
`.tdf` 는 **ZIP 아카이브**이며 그 안에 다수의 `.dd` 파일이 들어있다.

`.dd` 는 **두 가지 형식**이 섞여 존재한다.

> **검증 상태 (2026-07-07)**: 실코퍼스 605개 `.dd` 전수 —
> 체크섬 605/605 일치, 백트래킹 검증 파서(`dd_strict.py`)에서 **605/605
> 구조 유일해**(모호 0, 오류 0), 휴리스틱 파서(`dd_main.py`)와 100% 일치.
> 아래 규칙만으로 코퍼스의 모든 바이트가 설명된다.

> **✅ pure-A1 확정 (2026-07, `dd_prove.py measure`)**: 실코퍼스 **1255개**
> 전수 측정 결과 —
> - `walk 실패 0` (1255/1255 가 룩어헤드 없는 pure 문법으로 경계까지 완주)
> - 판별 카운터 [1]~[5] **전부 0**: wide 생략 0 · 폭1 앵커 생략 0 · 폭1 END
>   생략 0 · 폭1 explicit `04 00` 0 · 폭0 태그 뒤 비정상 0. (참고: 0x06
>   explicit 4B 제로도 0.)
>
> **의미**: 실데이터는 전부 **pure 이미지 안**에 있다. 즉 아래 "예외 4가지"와
> 생략 룩어헤드/정규식은 **실제로는 하나도 필요 없다** — 공리 A1(값이 태그를
> 고른다: 태그=폭 사다리, 0=폭0) + A2(원소 문법 `named_container | bare_value`)
> **두 줄로 코퍼스 전 바이트가 결정적으로(해 유일) 디코딩된다**. 예외들은
> 규칙이 아니라 "인코더는 최소 폭을 쓴다"의 그림자였음이 실측으로 확정.
> (구현: `dd_unified.py` pure 모드. 스펙의 예외 서술은 역사적 맥락으로 보존.)
>
> 남는 미결은 인코딩 구조가 아니라 **값 의미**뿐 — 0x05/0x06 의 부호(int16 vs
> uint16: 음수·경계값 미관측), 0x01 의 정체, 0x07(float32) 미관측. 이들은 새
> 덤프에 해당 값이 나와야 풀린다(measure 로는 판별 불가·직교).

---

## 1. 바이너리 TLV 형식

### 1.1 전체 레이아웃

```
+------------------+
| MAGIC (4 bytes)  |  17 FA AE 4E
+------------------+
| STRUCT (0x0B)    |  최상위 구조체 시작
|   ...필드들...    |
| END (0x00)       |  최상위 구조체 종료
+------------------+
| TRAILER (4 bytes)|  체크섬 (예: BC 94 26 A8)
+------------------+
```

- 파일은 `17 FA AE 4E` 매직으로 시작한다.
- 파일 끝 **4바이트는 트레일러(체크섬)** 이며 구조에서 제외한다.
- **체크섬 알고리즘 (확정)**: 트레일러 직전까지의 바이트(매직 포함, 트레일러
  자체는 제외)를 순서대로 누적한다. `h = 0` 에서 시작해 각 바이트 `b` 에
  대해 `h = (h*17 + b) mod 2^32` 를 적용하고, 최종 `h`(32비트)를
  **big-endian** 4바이트로 기록한 것이 트레일러.

  ```python
  h = 0
  for byte in raw[:-4]:
      h = (h * 17 + byte) & 0xFFFFFFFF
  assert h == int.from_bytes(raw[-4:], 'big')
  ```

  §1.4 예시에 적용하면 `bc 94 26 a8` 이 정확히 재현되고, 실코퍼스
  605/605 전수 일치 확인(2026-07-07). CRC 계열이 아니라 base-17 곱셈
  롤링 해시라서 표준 체크섬 도구로는 검출되지 않는다.

  > 주의: 이 해시는 CRC 보다 오류 검출력이 약하다(예: 뒤에서 k번째 바이트의
  > +17 변화와 k+1번째 바이트의 −1 변화가 상쇄 가능). 무결성 확인 용도로는
  > 충분하지만 위변조 방지 용도는 아니다.

### 1.2 TLV 필드 구조

각 필드는 다음 순서로 구성된다:

```
+----------+-----------------+----------+------------+
|  Type    |  Name           |  SubType |  Value     |
| (1 byte) | (string, NUL종료)| (1 byte) | (가변)      |
+----------+-----------------+----------+------------+
```

- **Type** = `0x09`(STRING) : 필드가 "이름"으로 시작함을 의미
- **Name** = NUL(`0x00`) 종료 ASCII 문자열 (필드명)
- **SubType** = 값의 타입 (아래 타입 표)
- **Value** = SubType 에 따른 값 (스칼라/문자열/컨테이너)

컨테이너(ARRAY/STRUCT)는 자식 필드들을 담고 **END(`0x00`)** 로 닫힌다.

### 1.3 타입 태그 표

| 태그 | 이름 | 크기 | 값 인코딩 |
|------|------|------|-----------|
| `0x00` | END | - | 블록(컨테이너) 종료 |
| `0x01` | INT8_A | 1B | 부호있는 8비트 정수 — 코퍼스 이름 필드에서 미관측, 희소(§4) |
| `0x02` | INT8_B | 0~1B | **0 전용 마커로 관측** — 이름 필드 602/602 이 값 0, 배열에서는 payload 없는 0 마커(§1.5) |
| `0x03` | UINT8 | 0~1B | **0 전용 마커로 관측** — 이름 필드 50/50 이 값 0, 배열에서 0x02 와 동일하게 동작(§1.5) |
| `0x04` | INT8_C | 1B | 부호있는 8비트 정수 — **실제 int8 값 캐리어** (측정: 비영값 1087건, 33종) |
| `0x05` | ENUM | 2B | 열거형 (int16 로 저장, big-endian) — 배열 원소로 반복 관측 |
| `0x06` | BOOL | 4B | 고정 4바이트 값(int32, big-endian) — 확정. `"TRUE"`/`"FALSE"` 문자열이 아님 (그건 0x09 STRING) |
| `0x07` | FLOAT32 | 4B | IEEE754 단정도 (big-endian) — 코퍼스 이름 필드에서 미관측 |
| `0x08` | FLOAT64 | 8B | IEEE754 배정도 (big-endian) |
| `0x09` | STRING | 가변 | NUL 종료 문자열 |
| `0x0A` | ARRAY | 가변 | 배열, END 로 닫힘 — 원소 인코딩은 §1.5 |
| `0x0B` | STRUCT | 가변 | 중첩 구조체, END 로 닫힘 |

> **측정 요약** (`dd_analyze.py`, 605개 파일의 "이름 있는 스칼라 필드" — 배열
> 모호성이 전혀 없는 위치): `0x02` 602개 전부 0 · `0x03` 50개 전부 0 ·
> `0x04` 1087개 전부 비영(33종) · `0x05` 250개 비영(35종) · `0x06` 1개(0) ·
> `0x08` 3450개 중 비영 3098(913종) · `0x01`/`0x07` 미관측.
> → `0x02`/`0x03` 은 "0 표기 전용", 실제 int8 값은 `0x04` 가 싣는다.
>
> **측정 스코프 주의(2026-07 추가)**: 이 수치는 `dd_analyze.py` 가 **매직이
> 있는 바이너리 파일만** 순회한 결과다(`raw[:4] != MAGIC` 이면 skip). 매직
> 없는 변형 파일의 필드는 이 통계에 안 잡힌다. 그래서 아래 §4 의 실측 사례
> 두 개가 이 요약과 겉보기 모순처럼 보인다:
> - **`0x06`**: 여기선 "1개(0)"인데 §4 는 `logical_scan_id = 129366`(비영)을
>   실측이라 한다. 둘이 같은 통계 범위라면 `0x06` 은 최소 2개·비영 ≥1 이어야
>   한다. 즉 129366 사례는 **매직 없는(또는 다른 스코프의) 파일**에서 나온
>   것으로 봐야 앞뒤가 맞는다.
> - **`0x01`**: 여기선 "미관측"인데 §4 는 "struct 멤버 위치 1회 관측"이라 한다
>   (struct 멤버 = 이름 필드). 역시 스코프 차이로 설명된다.
>
> 이 스코프는 원래 문서에 명시돼 있지 않았다(적대 검증에서 드러남). 확정하려면
> 매직 유무를 포함해 재측정 필요 — `0x06`/`0x01` 사례 파일의 매직을 확인하면
> "BOOL→int32(§4)" 근거의 출처가 분명해진다. 그전까지 이 둘은 "스코프 미상"으로
> 둔다.

> **주의(2026-07-07 정정)**: `0x02`/`0x04`는 과거 INT16(2B)/INT32(4B)로 문서화됐으나
> 실측 재확인 결과 각각 **1바이트** 정수다.

> **주의**: 모든 다바이트 수치는 **big-endian** 이다.

> **주의(0값 payload 생략 — 이름 있는 필드)**: 스칼라 SubType(`0x01~0x08`) 뒤에
> 고정폭 payload 가 항상 오는 건 아니다. 값이 `0`/`FALSE`(예: 미사용 detector
> 슬롯의 `nr_of_used_samples` 등)일 때는 **payload 전체가 생략**되고, SubType
> 태그 바로 뒤에 다음 필드의 `Name`(`0x09`+식별자+NUL) 또는 컨테이너를 닫는
> END(`0x00`)가 곧바로 이어진다. 이걸 모르고 고정폭을 그대로 소비하면 디싱크가
> 발생한다. 파서 규칙:
> - SubType 뒤에 **다음 필드 앵커**(`0x09`+식별자+NUL)가 바로 보이면 →
>   payload 미소비, 값 `0`.
> - SubType 뒤에 바로 **END(`0x00`)** 가 오면(컨테이너의 마지막 멤버) →
>   **1바이트 폭 태그(0x01~0x04)에 한해** 값 `0`, 바이트 미소비.
>   1바이트 폭에서는 "생략"과 "명시적 0x00 값 뒤 END"가 구분 불가지만 어느
>   쪽이든 값이 0 이라 안전하다. enum(2B)/bool(4B)/float(4~8B)는 첫 바이트가
>   `0x00` 이어도 나머지에 실값이 있을 수 있어(예: float 비정규값) 이 END
>   검사를 적용하지 않는다.

### 1.4 예시 (실측)

hex:
```
17 fa ae 4e 0b 09 "IFEUCM_machine_configuration_struct" 00
   0b 09 "lens_magnification" 00 08 3f d0 00 00 00 00 00 00
      09 "if_sim_mode" 00 09 "SIM_DISABLED" 00
      09 "hw_timestamp_transmission" 00 09 "TRUE" 00
   00 00
bc 94 26 a8
```

파싱 결과:
```json
{
  "IFEUCM_machine_configuration_struct": {
    "lens_magnification": 0.25,
    "if_sim_mode": "SIM_DISABLED",
    "hw_timestamp_transmission": "TRUE"
  }
}
```

### 1.5 배열 원소 인코딩 (2026-07-07 실측 확정)

배열(`0x0A`)의 원소는 **`SubType(1B) + payload` 의 반복이며 기본적으로 이름이
없다.** END(`0x00`)로 닫히는 것은 동일. 원소는 **같은 카테고리**(수치 /
문자열 / 컨테이너)를 유지한다.

**(1) `0x09` 원소는 이형(二形)** — 이름 NUL **직후 바이트**로 판별:

| post-NUL 바이트 | 해석 | 실측 예 |
|---|---|---|
| `0x0A`/`0x0B` (컨테이너) | **이름있는 컨테이너 원소** `{이름: struct/array}` | `detector = [{abc: {...}}, ...]` 맵 |
| 그 외 | **bare 문자열 값** | `valid_values = ["TRUE", "TRUE", ...]` |

```
0a 09 "TRUE" 00 09 "TRUE" 00 ... 00     → ["TRUE", "TRUE", ...]
0a 09 "abc" 00 0b ... 00 00             → [{"abc": {...}}]   ← 이름+struct
```

> 판별이 결정적인 이유: 이름있는 원소를 bare 문자열로 읽으면 바로 뒤의
> 컨테이너가 카테고리 위반이 되어 정합 불가. 역으로 bare 문자열들을 이름
> 원소로 읽으면 원소를 **둘씩 짝지어** `{TRUE: "TRUE"}` 로 먹는 오독이
> 생긴다(짝수 개면 조용히 잘못 파싱). post-NUL 판별이 둘 다 차단.

**(2) 배열의 0 값은 `0x02`/`0x03` 마커로 기록된다** — payload 없이 태그
1바이트만 소비, 값 0. 지배 타입에 영향 없음.

```
es_values 실측 (enum 파형 속 0 드롭아웃):
   ... 05 23 3d │ 02 │ 05 19 19 │ 05 18 a8 ...
       └ 9021 ─┘ └0┘ └ 6425 ──┘ └ 6312 ──┘

position_nrs 실측 (int8 값 배열, 값 캐리어는 0x04):
   0a │ 02 02 03 │ 04 02 │ 04 02 │ 04 03 │ 04 03 │ 04 04 │ …
      └─마커 0×3─┘ └── 0x04 값 페어: 2,2,3,3,4,… ──
   → [0, 0, 0, 2, 2, 3, 3, 4, 5, 5, …]  (단조증가)
```

- 측정 근거는 §1.3 측정 요약 — `0x02`/`0x03` 은 이름 필드에서도 전부 0.
- 이 규칙이 배열 모호성을 원천 제거한다: 전부-`02` 배열(`02 02 02 02` …)은
  "int8 값 2 들"이 아니라 **전부 0** 이다 (예: 빈 `logged_errors`).
  이를 몰랐던 파서는 `02 02` 를 (태그+payload)=2 로 둘씩 짝지어 먹는다.

**(3) 수치 배열은 값 크기에 따라 타입이 승격된다** — 배열은 *엄격 동질*이
아니라 *카테고리 동질*이다:

```
ratios 실측 (+5 등차수열):
   04 22 04 27 … 04 7c │ 05 00 81 05 00 86 …
   └ int8_c: 34,39,…,124 ┘ └ enum: 129,134,… ┘
   → [34, 39, …, 119, 124, 129, 134, …]
      └ int8 범위(≤127) ┘ └ 128↑ 는 enum(2B)로 승격 ┘
```

- 원소는 각자의 **자기 태그**로 폭이 결정되므로 승격이 섞여도 파싱은
  결정적이다. 수치 태그(`0x04~0x08`)는 한 카테고리로 묶여 폭이 달라도
  섞일 수 있다.
- 카테고리 교차(수치↔문자열↔컨테이너)는 실배열에 없다 → 디싱크 신호로
  취급한다.

---

## 2. 텍스트 형식

메모장으로 열면 사람이 읽을 수 있는 중첩 구조 텍스트.

```
{
  IFXATA_STRUCT = {
    logn_id = 2,
    phys_id = 5,
    detector = [
      { name = TRUE, intensities = [1, 2, 3] },
      { name = FALSE, vals = [] }
    ],
    empty = {}
  }
}
```

### 문법 (BNF 유사)

```
value  := struct | array | scalar
struct := '{' ( member (',' member)* )? ','? '}'
member := KEY '=' value
array  := '[' ( value (',' value)* )? ']'
scalar := number | TRUE | FALSE | <undef> | "quoted" | bareword
```

- `=` 앞뒤 공백은 무관 (`phys_id =5` 도 허용)
- 스칼라 타입 변환: 정수/실수(1.0e-3 포함)/불리언/`<undef>`→null/문자열
- 빈 구조체 `{}`, 빈 배열 `[]` 허용
- 배열 원소로 구조체 가능

---

## 3. 형식 자동 감지 규칙

`dd_main.py` 의 `detect_format()` 판정:

1. 앞 4바이트가 매직(`17 FA AE 4E`) → **바이너리**
2. NUL 바이트가 있고 출력가능문자 비율 < 85% → **바이너리** (매직 없는 변형 대비)
3. 그 외 → **텍스트**

---

## 4. 해결됨 / 미해결

### 해결됨 (2026-07-07 기준)

- **트레일러 체크섬**: `h = h*17 + b mod 2^32`, big-endian — §1.1. 605/605 검증.
- **BOOL 인코딩**: `0x06` = 고정 4바이트 int32 확정 (실측
  `logical_scan_id` payload `00 01 f9 56` = 129366). 파일에서 보이는
  `"TRUE"`/`"FALSE"` 는 별개의 STRING(`0x09`) 값이다.
- **ARRAY 원소 프레이밍**: §1.5 로 확정 — bare `SubType+payload` 반복이
  기본, `0x09` 원소는 post-NUL 바이트로 이형 판별, 이름있는 원소는
  `이름+컨테이너` 형태만 존재.
- **스칼라 payload 생략(0값, 이름 필드)**: §1.3 주의 참고 — 다음 필드
  앵커 검사(전 스칼라) + END-직전 검사(1바이트 태그 한정)로 처리.
  실측 예: `nr_of_used_samples`(앵커 케이스, 오프셋 `0x021f`),
  `first_index`/`last_index`/`repeat_count`(END-직전 케이스,
  `0x4b8c~0x4bb6` 오프셋 산술로 확인).
- **배열의 0 표기**: 생략이 아니라 **전용 마커 태그** `0x02`/`0x03` — §1.5(2).
- **`0x02`/`0x04` 크기**: 둘 다 1B 확정 (과거 2B/4B 표기는 오류).
- **`0x02`/`0x03`/`0x04` 의 역할 구분**: 측정으로 확정 — `0x02`/`0x03` 은
  "0 전용"(이름 필드 652개 전부 0), `0x04` 가 실제 int8 값 캐리어.
- **수치 배열의 타입 혼합**: 값 크기에 따른 승격(int8→enum) — §1.5(3).

### 미해결 / 확인 필요

- **`0x01`(INT8_A) 의 용도**: 코퍼스 이름 필드에서 미관측. 설정 파일
  1곳(`IDCM_machine_configuration_struct`)의 struct 멤버 위치에서 1회
  관측 — 실사례가 쌓이면 §1.3 에 근거 추가.
- **`0x07`(FLOAT32) 실측 미관측**: 폭 4B 는 스펙 추정 유지.
- **enum 값의 의미**: 값→라벨 매핑은 포맷 외 정보(장비 소프트웨어)로 추정.
- **`0x02`/`0x03` 이 비영 값을 싣는 반례**: 현 코퍼스(605개)에서 0건.
  > **정정(2026-07, 적대 검증)**: 예전엔 "새 덤프에서 나타나면 `dd_strict.py`
  > 가 모호/불일치로 즉시 검출한다"고 적었으나 **이름 필드 케이스에선 거짓**이다.
  > 예: `09 'a' 00 02 05`(0x02 뒤 비영 payload)를 `dd_strict.py` 는 경고 없이
  > 깨끗한 **unique** `{a:5}` 로 반환한다(스펙 표가 `0x02` 를 "0~1B"로 허용하고,
  > 오라클 문법도 그 payload 해석을 유효한 유일해로 받기 때문). 즉 이 자리엔
  > 트립와이어가 없다. **실질 트립와이어는 `dd_unified.py` 의 `anomalies`** 다
  > — 폭0 태그(02/03) 뒤가 필드 경계가 아니면 A1 확정 위반 후보로 기록한다.
  > (배열 케이스는 폭0 마커로 읽혀 값에 영향이 없고, 지배 타입과 다른 폭0 태그가
  > 섞이는 것뿐이라 애초에 모호가 아니다.)
- 스펙 외 태그가 나오면 `dd_strict.py`(실패 진단 + hex 문맥) 또는
  `dd_analyze.py`(태그 의미론 측정)로 컨텍스트를 수집해 확인.

### 적대 검증에서 드러난 코드/문서 결함 (2026-07, 수정 완료)

독립 검증에서 나온 실제 결함들과 조치:

- **`dd_main.py` 잠복 버그 — `04 00`(명시적 int8 값 0) 뒤 형제 필드 유실**:
  END-직전 생략 휴리스틱이 `0x04`(값 캐리어)에도 발동해 `0x00` payload 를
  END 로 오인, 컨테이너를 조기 폐쇄하고 뒤 필드를 무증상으로 잃었다. →
  생략 검사를 **0x01/0x02/0x03 에 한정**(0x04 제외)하고, `parse_bytes` 에
  경계(n−4) 미도달을 알리는 `boundary_ok`/`trailing_bytes` 추가. `dd_unified`
  도 같은 결함을 공유했어서 함께 수정.
- **`dd_main.py` CLI 실행 불가**: `main()` 이 미정의 `_run()` 을 호출하고
  `__main__` 가드도 없었다. → `_run()` 구현 + `detect_format()`(§3) 실제 추가
  + `if __name__ == '__main__'` 가드 추가.
- **`detect_format()` 유령 함수(§3 참조)**: 코드에 없던 것을 §3 규칙대로 구현.
- **`dd_analyze.py` 무증상 절단**: END-생략을 전 폭에 적용(스펙은 1B 한정)해
  명시적 `0.0` float64·값≤255 named 수치를 오분류하고 walk 를 조기 종료,
  후속 필드를 통계에서 조용히 누락시켰다. → END-생략을 `size==1` 로 제한하고,
  walk 종료 위치가 경계와 다르면 오류로 올려 `parse_errors` 에 잡히게 함.
- **두 "pure" 구현 불일치**: `dd_unified._array` 가 named_container 판별에서
  이름 유효성(`NAME_RE`)을 안 봐 `dd_strict`/`enum_parses` 와 같은 바이트를
  다르게 읽었다. → `NAME_RE` 검사 추가로 결정화 일치. 배열 범주 동질성(§1.5-3)
  미검사도 `anomalies` 플래그로 보완(관대 디코더라 거부는 안 함).

---

## 5. 도구

### 운영 (현재 트리)

| 도구 | 역할 |
|---|---|
| `dd_main.py` | **디코더 본체** — .dd/.tdf(ZIP) 파싱 + 체크섬 검증(`dd_trailer_ok`) + 형식 감지(`detect_format`) + **진단(`diagnose`/`--diagnose`/`--at`)** + CLI(폴더/단일/`--raw`·JSON·스켈레톤·발굴). 실코퍼스 1255개에서 pure 문법과 동치 확인됨. |

#### 진단 기능 (`diagnose`) — 실패/이상 지점 탐색

파싱이 깨졌거나 결과가 이상할 때 **어디서·왜** 어긋났는지 파고드는 운영 내장
기능. 제거된 `dd_strict.py` 의 "실패 진단" 절반을 파서에 내재화한 것이다
(복구·독립 오라클 대조는 미포함).

- **첫 파손 지점**: 문제 후보(미지 태그 / 배열 카테고리 교차 / 경계 미도달 /
  파서 예외 / 체크섬 불일치) 중 **가장 앞선 오프셋**을 첫 derail 로 지목.
- **원인 매핑**: 증상을 스펙 §1.5 의 디싱크 신호에 매핑해 설명·제안 출력
  (예: 경계 미도달 + 직전 0x00 → "스칼라 0값을 END 로 오독한 조기 폐쇄" 안내).
- **주변 hexdump** + **바이트 다중해석**(`interpret_payload`, int8/16/32·
  float32/64·string 후보)으로 그 지점 바이트의 의미를 직접 확인.
- **관측 훅**: `TLVParser.anomalies` 가 배열 카테고리 교차(§1.5-3 위반: 수치↔
  문자열↔컨테이너 혼입)를 기록. **순수 관측이라 파싱 출력은 불변**.
- **수동 조사**: `--at OFFSET` 은 파서가 성공 판정이어도 그 오프셋 창을 강제로
  덤프·해석("여기가 이상하다" 싶은 지점 직접 조사).

```
python3 dd_main.py --raw <파일>.dd --diagnose      # 자동: 첫 파손 지점 진단
python3 dd_main.py --raw <파일>.dd --at 0x1a4       # 수동: 특정 오프셋 조사
python3 dd_main.py <폴더|.tdf> --diagnose           # TDF 내 각 .dd 진단
```

### 리버스엔지니어링/검증 도구 (제거됨 — git 이력에서 복구 가능)

포맷 규칙이 실코퍼스 1255개로 **pure-A1 확정**(상단 검증 상태 참조)된 뒤,
운영에 불필요한 아래 도구들은 트리에서 제거했다. 필요하면 git 이력의
커밋 `3e3ecb0`(제거 직전)에서 되살릴 수 있다.

| 도구 | 역할 (당시) |
|---|---|
| `dd_strict.py` | 백트래킹 검증 파서 — 전역 검증(경계/유일해) + 실패 진단·복구(salvage) |
| `dd_unified.py` | 공리 A1·A2 파서 — "예외 4개 = 공리 2개" 환원의 참조 구현(safe/pure) |
| `dd_prove.py` | 증명 러너 — 생성모델 차분·오라클 교차·반례·퍼즈·`measure`(pure-A1 판별) |
| `dd_analyze.py` | 태그 의미론 코퍼스 측정(읽기 전용) — §1.3 측정 요약의 출처 |
| `trailer_probe.py` | 트레일러 체크섬 표준 알고리즘 그리드 탐색 |
| `trailer_solve.py` | 체크섬 역산 솔버(GF(2) GCD / 곱셈 해시 복원) — §1.1 확정의 출처 |

> 시각화 문서: `decoding_rules.html`(디코딩 룰), `byte_structure.html`(바이트
> 구조·예외의 공리 환원), `reading_model.html`(값이 태그를 고른다 — 새 해석)
> 는 포맷 참조 문서로 유지한다.
